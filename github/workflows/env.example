/* UI ONLY — no backend */
/*
  Example environment variables for local UI dev.
  Do NOT put secrets here. This is UI-only (no backend).
*/
NEXT_PUBLIC_APP_NAME=LabourMandi
NEXT_PUBLIC_API_BASE_URL=https://example.com/api-not-used
