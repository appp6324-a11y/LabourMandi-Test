/* UI ONLY — no backend */

# LabourMandi — Frontend (UI only)

This repository contains the frontend skeleton for the LabourMandi UI built with Next.js (App Router), TypeScript, Tailwind CSS and a ShadCN-like component skeleton.

**Important**: UI ONLY — no backend or real API calls are performed anywhere in this scaffold.

## Quick start

1. Install dependencies:
```bash
npm i
